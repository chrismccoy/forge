================================================================================
  FORGE  -  Docs & Diagrams          ( /forge-docs )          12 tools, 4 pages
================================================================================
  Onboarding docs, Mermaid, README, changelog, prompt tools, code annotation.

  STEP 2 - pick a tool

  .------------------------------------------------------------------------------.
  | [Tool]                                                 Which tool?  (pg 1/4) |
  |------------------------------------------------------------------------------|
  |  ( 1 )  Onboarding documentation                       -> /explain-my-code   |
  |         One self-contained 13-section CODEBASE_DOCUMENTATION.md for a repo.  |
  |                                                                              |
  |  ( 2 )  Codebase to Mermaid                          -> /codebase-to-mermaid |
  |         Validated Mermaid diagrams with file:line citations.                 |
  |                                                                              |
  |  ( 3 )  Mermaid to ASCII                               -> /mermaid-to-ascii  |
  |         Convert a Mermaid file into a monospace ASCII diagram (.txt).        |
  |                                                                              |
  |  ( 4 )  More...                                                              |
  |         Sequence diagrams, README, changelog.  -> page 2                     |
  |                                                                              |
  |  [Other] type a tool name ................................................   |
  '------------------------------------------------------------------------------'

  .------------------------------------------------------------------------------.
  | [Tool]                                                 Which tool?  (pg 2/4) |
  |------------------------------------------------------------------------------|
  |  ( 1 )  Mermaid sequence diagram                       -> /mermaid-sequence  |
  |         Turn bullet-point process steps into one valid Mermaid sequence      |
  |         diagram.                                                             |
  |                                                                              |
  |  ( 2 )  Write a README                                 -> /readme-builder    |
  |         Scan a repo and write a beginner-friendly README.md.                 |
  |                                                                              |
  |  ( 3 )  Generate a changelog                         -> /changelog-generator |
  |         User-facing changelog built from actual diffs across git history.    |
  |                                                                              |
  |  ( 4 )  More...                                                              |
  |         Prompt explainer, analyzer, auditor.  -> page 3                      |
  |                                                                              |
  |  [Other] type a tool name ................................................   |
  '------------------------------------------------------------------------------'

  .------------------------------------------------------------------------------.
  | [Tool]                                                 Which tool?  (pg 3/4) |
  |------------------------------------------------------------------------------|
  |  ( 1 )  Explain a prompt (plain English)               -> /explain-prompt    |
  |         Describe any AI prompt in beginner-friendly plain English, eight     |
  |         fixed sections.                                                      |
  |                                                                              |
  |  ( 2 )  Analyze a prompt (deep dive)                   -> /analyze-prompt    |
  |         Rigorous review-ready breakdown: anatomy, techniques, failure modes, |
  |         improvements.                                                        |
  |                                                                              |
  |  ( 3 )  Audit prompt architecture                      -> /rank-prompt       |
  |         Tier and score a prompt across 8 dimensions, with evidence and one   |
  |         concrete improvement.                                                |
  |                                                                              |
  |  ( 4 )  More...                                                              |
  |         Table-only prompt auditor, stencil cutter, code annotator.  -> page 4|
  |                                                                              |
  |  [Other] type a tool name ................................................   |
  '------------------------------------------------------------------------------'

  .------------------------------------------------------------------------------.
  | [Tool]                                                 Which tool?  (pg 4/4) |
  |------------------------------------------------------------------------------|
  |  ( 1 )  Audit a prompt as a table                      -> /prompt-rank-table |
  |         The same 8-dimension audit as a tier, an evidence table,             |
  |         and a one-line verdict.  Nothing else.                               |
  |                                                                              |
  |  ( 2 )  Cut an image prompt stencil                    -> /prompt-stencil    |
  |         Turn a working image prompt into a reusable template: locks,         |
  |         variables, drift guards, filled proofs.                              |
  |                                                                              |
  |  ( 3 )  Annotate code for teaching                     -> /code-teacher      |
  |         Header block + line-by-line comments explaining what and             |
  |         why.  Code itself unchanged.                                         |
  |                                                                              |
  |  [Other] type a tool name ................................................   |
  '------------------------------------------------------------------------------'

  STEP 3 - run it
    read commands/<command>.md -> load lib/<tool>/SKILL.md -> "Running /<cmd>."


================================================================================
  STEP 4 - AFTER YOU PICK: the tool's own intake questions
================================================================================
  KEY   ( ) single-pick    ___ free-text
  Most of these run straight from a path - little or no intake.


-------------------------------------------------------- /explain-my-code -----
  NO questions when a path arg is given.  If empty, one routing question:

  .--[Target]-- Which codebase should I document? -----------------------------.
  |  ( ) Current working directory   ( ) Custom path (then asks path) ___      |
  '----------------------------------------------------------------------------'


----------------------------------------------------- /codebase-to-mermaid ----
  NO questions when a path arg is given.  If empty, one routing question:

  .--[Target]-- Which codebase should I map? ----------------------------------.
  |  ( ) Current working directory   ( ) Custom path (then asks path) ___      |
  '----------------------------------------------------------------------------'


-------------------------------------------------------- /mermaid-to-ascii -----
  NO menu.  If no file arg, one plain prompt (STOP for reply):

  .--[File]-- Which Mermaid file should I convert? (path or filename) ---------.
  |  ___ e.g. flow.mmd, diagram.mermaid, /abs/path/flow.mmd                    |
  '----------------------------------------------------------------------------'


---------------------------------------------------------- /readme-builder -----
  .--[Target]-- which project to document?  (skipped if a path arg passed) ----.
  |  ( ) the current directory   ( ) a different folder (give path) ___        |
  |  [Other]                                                                   |
  '----------------------------------------------------------------------------'
  .--[Output]-- what to do with the result?  (optional) -----------------------.
  |  ( ) write it to README.md in the project folder                           |
  |  ( ) print the Markdown so I can save it myself (default)                  |
  '----------------------------------------------------------------------------'


------------------------------------------------------ /changelog-generator ----
  NO questions.  Runs from the path arg, or the current directory if empty.
  Date/version ranges are rejected - always the full history.


-------------------------------------------------------- /mermaid-sequence ----
  NO menu.  If no bullet list, one plain prompt (STOP for reply):

  .--[Steps]-- Paste your bullet-point process steps --------------------------.
  |  ___ e.g. "* User submits form  * Server validates  * Server returns token"|
  '----------------------------------------------------------------------------'


---------------------------------------------------------- /explain-prompt ----
  NO menu.  If no prompt arg, one plain prompt (STOP for reply):

  .--[Prompt]-- Paste the prompt to explain, or give a file path ---------------.
  |  ___ the AI prompt you want described in plain, beginner English            |
  '-----------------------------------------------------------------------------'


---------------------------------------------------------- /analyze-prompt ----
  NO menu.  If no prompt arg, one plain prompt (STOP for reply):

  .--[Prompt]-- Paste the prompt to analyze, or give a file path ---------------.
  |  ___ the AI prompt you want a rigorous, review-ready breakdown of           |
  '-----------------------------------------------------------------------------'


--------------------------------------------------------------- /rank-prompt ---
  Submission comes from the arg, a file path, or a plain ask.  Two optional
  context questions after that; skipping either is fine.

  .--[Prompt]-- paste the prompt to audit, or give a file path ----------------.
  |  ___ plain ask, STOP for the reply.  An unreplaced placeholder counts      |
  |      as absent - no rank, no score, no table                               |
  '----------------------------------------------------------------------------'
  .--[Target model]-- what the prompt runs against  (optional) ----------------.
  |  ( ) Claude   ( ) GPT class   ( ) Gemini   ( ) Model-agnostic              |
  '----------------------------------------------------------------------------'
  .--[Current failure]-- what it is failing at today  (optional) --------------.
  |  ( ) Ignores parts of the instructions   ( ) Output format drifts          |
  |  ( ) Breaks on odd or hostile input   ( ) Nothing specific                 |
  '----------------------------------------------------------------------------'


------------------------------------------------------------ /prompt-stencil ---
  2 required fields, 1 optional.  At most ONE clarifying question overall.

  .--[Source prompt]-- the working image prompt, verbatim  (required) ---------.
  |  ___ plain ask or path.  Missing, unusable, or non-visual is never a       |
  |      question - it is the Cannot Build - Missing Material state            |
  '----------------------------------------------------------------------------'
  .--[Variable]-- what should become swappable?  (required) -------------------.
  |  ( ) Subject or product   ( ) Palette accent                               |
  |  ( ) Scene or setting   ( ) Aspect ratio or format                         |
  '----------------------------------------------------------------------------'
  .--[Constraints]-- up to two material constraints  (optional) ---------------.
  |  ___ asked once, in one line; a skip records "None"                        |
  '----------------------------------------------------------------------------'
--------------------------------------------------------- /prompt-rank-table ---
  Submission comes from the arg, a file path, or a plain ask.  Two optional
  context questions after that; skipping either is fine.

  .--[Prompt]-- paste the prompt to audit, or give a file path ----------------.
  |  ___ plain ask, STOP for the reply.  An unreplaced placeholder counts      |
  |      as absent - no rank, no score, no table                               |
  '----------------------------------------------------------------------------'
  .--[Target model]-- what the prompt runs against  (optional) ----------------.
  |  ( ) Claude   ( ) GPT class   ( ) Gemini   ( ) Model-agnostic              |
  '----------------------------------------------------------------------------'
  .--[Current failure]-- what it is failing at today  (optional) --------------.
  |  ( ) Ignores parts of the instructions   ( ) Output format drifts          |
  |  ( ) Breaks on odd or hostile input   ( ) Nothing specific                 |
  '----------------------------------------------------------------------------'
  .--[Output]-- three sections, nothing else ----------------------------------.
  |  Overall Rank . Skill Signals Table (always 8 one-line rows) . Verdict.    |
  |  Same engine as /rank-prompt; only the report shape differs.               |
  '----------------------------------------------------------------------------'


-------------------------------------------------------------- /code-teacher ---
  NO menu until code is in hand.  If no code arg, one plain prompt (STOP for
  reply) that also asks for the bugs and design decisions behind the code.

  .--[Code]-- paste the code to annotate, or give a file path -----------------.
  |  ___ plain ask, STOP for the reply.  Not source code, or code built to     |
  |      do harm, means it says so and stops                                   |
  '----------------------------------------------------------------------------'
  .--[Audience level]-- who the comments are pitched at  (optional) -----------.
  |  ( ) Beginner in this language   ( ) Intermediate (default)                |
  |  ( ) Experienced, new to this codebase                                     |
  '----------------------------------------------------------------------------'
  .--[Dev notes]-- bugs hit, surprises, ordering that mattered  (optional) ----.
  |  ( ) Nothing to add   ( ) I'll describe them                               |
  |  Anything you state is written as fact; anything inferred carries          |
  |  INFERRED: and must point at a real line.                                  |
  '----------------------------------------------------------------------------'
  .--[Output]-- four sections, fixed order ------------------------------------.
  |  Plan (<=8 lines) . one fenced code block . Teaching Points (3-5) .        |
  |  Verification.  Original code returned unchanged, comments only.           |
  '----------------------------------------------------------------------------'
================================================================================
